<h1><%= @payment_method.name -%></h1>

<ul>
   <% @payment_method.students.each do |c| %>
   <li><%= link_to c.name, :action => "show", :id => c.id -%></li>
   <% end %>
</ul>

<%= link_to 'Back', {:action => 'list' } %>